#include <algorithm>
#include <vector>
#include <iostream>
#include <functional>
#include <queue>
#include <set>

using namespace std;

int barLength;

int main() {
    cin.tie(nullptr);
    ios::sync_with_stdio(false);

    while (cin >> barLength) {
        int nbCuts:
        vector<int> cuts;
        cin >> cuts;

    }
}
